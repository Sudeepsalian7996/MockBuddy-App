import styled from "styled-components";

export const LogoWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 0.5rem;
  position: relative;
  left: 4rem;

  .header-logo {
    width: 3.5rem;
  }
`;
